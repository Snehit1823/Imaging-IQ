"use client";

import { motion, AnimatePresence } from "framer-motion";
import { X, AlertCircle, CheckCircle2, FileText, Share2, Activity, Zap } from "lucide-react";

interface ResultModalProps {
  isOpen: boolean;
  onClose: () => void;
}

export function ResultModal({ isOpen, onClose }: ResultModalProps) {
  return (
    <AnimatePresence>
      {isOpen && (
        <>
          {/* Backdrop with immersive blur */}
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            onClick={onClose}
            className="fixed inset-0 bg-slate-950/80 backdrop-blur-md z-[60]"
          />

          {/* Main Result Window */}
          <motion.div
            initial={{ opacity: 0, scale: 0.9, y: 30 }}
            animate={{ opacity: 1, scale: 1, y: 0 }}
            exit={{ opacity: 0, scale: 0.9, y: 30 }}
            className="fixed top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-full max-w-5xl bg-slate-900 border border-slate-800 rounded-[2.5rem] z-[61] overflow-hidden shadow-2xl"
          >
            <div className="grid grid-cols-1 lg:grid-cols-2 h-full lg:h-[650px]">
              
              {/* --- LEFT: RADIOLOGIC CANVAS --- */}
              <div className="relative bg-black flex items-center justify-center p-6 border-b lg:border-b-0 lg:border-r border-slate-800">
                <div className="absolute top-6 left-6 z-20 flex flex-col gap-2">
                  <span className="px-3 py-1 bg-red-500/20 border border-red-500/40 rounded-full text-red-400 text-[10px] font-black uppercase tracking-widest">
                    Anomaly Detected
                  </span>
                  <span className="px-3 py-1 bg-slate-800/80 backdrop-blur-md border border-slate-700 rounded-full text-slate-300 text-[10px] font-bold uppercase tracking-widest">
                    Scan ID: #IQ-2026-X
                  </span>
                </div>
                
                <div className="relative w-full h-full rounded-2xl overflow-hidden group">
                  <img 
                    src="https://images.unsplash.com/photo-1530210124550-912dc1381cb8?auto=format&fit=crop&q=80&w=1000" 
                    alt="X-ray Analysis"
                    className="object-contain h-full w-full opacity-60 transition-opacity group-hover:opacity-80"
                  />

                  {/* 🔴 AI HEATMAP GLOW: Visual Explainability */}
                  <motion.div 
                    animate={{ opacity: [0.2, 0.5, 0.2], scale: [1, 1.1, 1] }}
                    transition={{ duration: 3, repeat: Infinity }}
                    className="absolute top-[35%] right-[25%] w-40 h-40 bg-red-500/30 blur-[60px] rounded-full"
                  />
                  
                  {/* Bounding Box Overlay */}
                  <div className="absolute top-[38%] right-[28%] w-28 h-28 border-2 border-dashed border-red-500/60 rounded-xl shadow-[0_0_15px_rgba(239,68,68,0.3)]" />
                </div>
              </div>

              {/* --- RIGHT: CLINICAL INTELLIGENCE --- */}
              <div className="p-10 flex flex-col bg-slate-900/50">
                <div className="flex justify-between items-start mb-8">
                  <div>
                    <h2 className="text-3xl font-black text-white tracking-tighter italic">Diagnostic Insights</h2>
                    <p className="text-slate-500 text-xs font-bold uppercase tracking-widest mt-1">Generated by ImagingIQ Neural Core</p>
                  </div>
                  <button onClick={onClose} className="p-2 hover:bg-slate-800 rounded-xl transition-colors text-slate-500 hover:text-white">
                    <X className="w-6 h-6" />
                  </button>
                </div>

                <div className="space-y-6 flex-1">
                  {/* Automated Diagnosis Tile */}
                  <div className="p-6 rounded-3xl bg-gradient-to-br from-red-500/10 to-transparent border border-red-500/20 shadow-lg shadow-red-500/5">
                    <div className="flex items-center gap-2 mb-3">
                      <AlertCircle className="w-4 h-4 text-red-400" />
                      <span className="text-xs font-black text-red-400 uppercase tracking-widest">Findings</span>
                    </div>
                    <h4 className="text-xl font-bold text-white mb-2">Left Lobe Consolidation</h4>
                    <p className="text-sm text-slate-400 leading-relaxed">
                      Radiographic patterns strongly suggest localized pulmonary infiltration. High correlation with clinical pneumonia markers.
                    </p>
                  </div>

                  {/* Confidence Bento Stats */}
                  <div className="grid grid-cols-2 gap-4">
                    <div className="p-5 rounded-3xl bg-slate-800/40 border border-slate-700 hover:border-cyan-500/30 transition-colors">
                      <span className="text-[10px] font-black text-slate-500 uppercase tracking-widest block mb-1">AI Confidence</span>
                      <span className="text-3xl font-black text-cyan-400">98.4%</span>
                    </div>
                    <div className="p-5 rounded-3xl bg-slate-800/40 border border-slate-700 hover:border-orange-500/30 transition-colors">
                      <span className="text-[10px] font-black text-slate-500 uppercase tracking-widest block mb-1">Priority</span>
                      <span className="text-3xl font-black text-orange-400">STAT</span>
                    </div>
                  </div>

                  {/* Secondary Heuristics */}
                  <div className="space-y-2">
                    <SecondaryFinding label="Pleural Effusion" status="Not Detected" />
                    <SecondaryFinding label="Pneumothorax" status="Not Detected" />
                  </div>
                </div>

                {/* Interaction Footer */}
                <div className="mt-8 flex gap-4">
                  <button className="flex-1 py-4 bg-cyan-500 hover:bg-cyan-400 text-slate-900 font-black rounded-2xl transition-all shadow-xl shadow-cyan-500/20 uppercase tracking-widest text-xs flex items-center justify-center gap-2 active:scale-95">
                    <FileText className="w-4 h-4" /> Download Report
                  </button>
                  <button className="p-4 bg-slate-800 hover:bg-slate-700 text-white rounded-2xl border border-slate-700 transition-all active:scale-95">
                    <Share2 className="w-5 h-5" />
                  </button>
                </div>
              </div>
            </div>
          </motion.div>
        </>
      )}
    </AnimatePresence>
  );
}

function SecondaryFinding({ label, status }: { label: string; status: string }) {
  return (
    <div className="flex items-center justify-between p-3 rounded-2xl bg-slate-800/20 border border-white/5">
      <div className="flex items-center gap-2 text-sm text-slate-300">
        <CheckCircle2 className="w-4 h-4 text-emerald-500" /> {label}
      </div>
      <span className="text-[10px] text-slate-500 font-black uppercase">{status}</span>
    </div>
  );
}